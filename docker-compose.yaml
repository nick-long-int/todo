version: "3.8"

services:
  postgres:
    image: postgres:latest
    container_name: todo-db
    ports:
      - 5431:5432
    environment:
      - POSTGRES_DB=task
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin

  redis:
    container_name: redis-todo
    image: redis:latest
    ports:
      - 6379:6379
    restart: always
